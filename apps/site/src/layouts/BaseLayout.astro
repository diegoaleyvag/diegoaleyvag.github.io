---
import "../styles/tokens.css";
import "../styles/fonts.css";
import "../styles/base.css";
import "../styles/ledger.css";
import "../styles/shell.css";
import SiteFooter from "../components/SiteFooter.astro";
import SiteHeader from "../components/SiteHeader.astro";
import { SITE_SHELL_COPY } from "../components/copy";
import { loadResume } from "@portfolio/resume";

interface Props {
  title: string;
  description?: string;
  activePath: "/" | "/resume/" | null;
}

const { title, description, activePath } = Astro.props;
const resume = await loadResume();
const brandName = resume.view.identity.name.value;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="data:," />
    <link
      rel="preload"
      href="/fonts/archivo-variable.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href="/fonts/archivo-black.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <title>{title}</title>
    {description ? <meta name="description" content={description} /> : null}
  </head>
  <body>
    <a class="skip-link" href="#main-content">
      {SITE_SHELL_COPY.skipToContent}
    </a>
    <SiteHeader activePath={activePath} brandName={brandName} />
    <main id="main-content">
      <slot />
    </main>
    <SiteFooter />
  </body>
</html>
