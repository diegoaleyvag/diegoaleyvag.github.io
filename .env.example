# Copy to .env for local use. Never commit a real .env file.
# GROQ_API_KEY is a secret: server/local env only — never expose it to the
# browser, a client bundle, or any NEXT_PUBLIC_*-style variable.
GROQ_API_KEY=
LLM_PROVIDER=fake
GROQ_MODEL=
LIVE_EXECUTION_ENABLED=false
DEMO_MODE=replay
